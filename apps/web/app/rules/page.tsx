"use client";
import { useEffect,useState } from "react";import Shell from "@/components/Shell";import { request } from "@/lib/api";
type Rule={id:string;external_id:string;name:string;severity:string;source_types:string[];required_fields:string[];mitre_techniques:string[]};
export default function Page(){const[data,setData]=useState<Rule[]>([]);useEffect(()=>{request<Rule[]>("/rules").then(setData)},[]);return <Shell><div className="header"><div><h1>Detection dependencies</h1><p className="subtitle">Rule-to-source, field and MITRE ATT&CK relationships.</p></div></div><div className="card"><table><thead><tr><th>Rule</th><th>Severity</th><th>Sources</th><th>Required fields</th><th>ATT&CK</th></tr></thead><tbody>{data.map(x=><tr key={x.id}><td>{x.name}<div className="subtitle">{x.external_id}</div></td><td>{x.severity}</td><td>{x.source_types.join(", ")}</td><td>{x.required_fields.join(", ")}</td><td>{x.mitre_techniques.join(", ")}</td></tr>)}</tbody></table></div></Shell>}
